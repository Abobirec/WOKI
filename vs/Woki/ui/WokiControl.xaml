<UserControl x:Class="Woki.UI.WokiControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="500" d:DesignWidth="320" Name="Root">
  <Grid Margin="6">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Шапка -->
    <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,6">
      <TextBlock DockPanel.Dock="Left" Text="WOKI" FontWeight="Bold" VerticalAlignment="Center" />
      <Button DockPanel.Dock="Right" Name="GearButton" Content="⚙" Width="28" Click="OnToggleSettings" />
    </DockPanel>

    <!-- Настройки -->
    <Border Grid.Row="1" Name="SettingsPanel" Visibility="Collapsed"
            BorderBrush="#55888888" BorderThickness="1" Padding="6" Margin="0,0,0,6">
      <StackPanel>
        <TextBlock Text="Провайдер" />
        <ComboBox Name="ProviderBox" SelectionChanged="OnProviderChanged">
          <ComboBoxItem Content="nvidia" />
          <ComboBoxItem Content="github" />
        </ComboBox>

        <TextBlock Text="Модель" Margin="0,6,0,0" />
        <TextBox Name="ModelBox" />

        <TextBlock Text="API-ключ" Margin="0,6,0,0" />
        <PasswordBox Name="KeyBox" />

        <Button Name="SaveSettings" Content="Сохранить настройки" Margin="0,8,0,0" Click="OnSaveSettings" />
      </StackPanel>
    </Border>

    <!-- Лента сообщений -->
    <ScrollViewer Grid.Row="2" Name="LogScroll" VerticalScrollBarVisibility="Auto">
      <ItemsControl Name="LogList">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Margin="0,3" Padding="6" CornerRadius="6" Background="#22888888">
              <StackPanel>
                <TextBlock Text="{Binding Role}" FontSize="10" Opacity="0.6" />
                <TextBlock Text="{Binding Text}" TextWrapping="Wrap" />
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <!-- Ввод -->
    <DockPanel Grid.Row="3" Margin="0,6,0,0">
      <Button DockPanel.Dock="Right" Name="SendButton" Content="Отправить" Width="80" Margin="6,0,0,0" Click="OnSend" />
      <TextBox Name="InputBox" AcceptsReturn="False" MinHeight="28"
               KeyDown="OnInputKeyDown" VerticalContentAlignment="Center" />
    </DockPanel>
  </Grid>
</UserControl>
